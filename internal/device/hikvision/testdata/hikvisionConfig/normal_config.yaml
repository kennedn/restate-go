apiVersion: v2
devices:
- type: hikvision
  config:
    name: front_camera
    timeoutMs: 3000
    host: "192.168.1.1"
    user: api
    password: password
    defaultMode: eventIntelligence
- type: hikvision
  config:
    name: back_camera
    timeoutMs: 3000
    host: "192.168.1.0"
    user: api
    password: password
    defaultMode: irLight